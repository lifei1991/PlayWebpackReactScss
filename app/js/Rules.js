var React = require('react');
var ReactDOM = require('react-dom');
var Rules = React.createClass({
  render: function() {
    return (
      <div>rules</div>
    );
  }
});

module.exports = Rules;
//ReactDOM.render(<Rule/>, document.getElementById('content'));

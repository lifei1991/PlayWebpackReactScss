var React = require('react');
var ReactDOM = require('react-dom');
var Wells = React.createClass({
  render: function() {
    return (
      <div>wells</div>
    );
  }
});

module.exports = Wells;
//ReactDOM.render(<Well/>, document.getElementById('content'));
